import React, { useState } from 'react'

const Tickets = ({nombre,area,descripcion_servicio}) =>{
 const [mostrat, setMostrar] = useState(false)

  return (
    <div>
      
    </div>
  )
}

export default Tickets
